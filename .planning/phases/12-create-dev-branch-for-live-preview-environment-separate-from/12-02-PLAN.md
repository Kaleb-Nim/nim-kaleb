---
phase: 12-create-dev-branch-for-live-preview-environment-separate-from
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified: [README.md]
autonomous: true
requirements: [DEV-04]
requirements_addressed: [DEV-04]
tags: [docs, readme, deployment]

must_haves:
  truths:
    - "README documents the three-tier branching model: main → production, dev → stable preview, feature branches → ephemeral PR previews"
    - "README lists the captured dev preview URL from Plan 12-01"
    - "A contributor reading the README can identify where to push code for each environment without asking"
  artifacts:
    - path: "README.md"
      provides: "Deployment section describing branching model + preview URL"
      contains: "main → production"
      contains_alt: "dev → "
      contains_alt2: "preview"
  key_links:
    - from: "README Deployment section"
      to: "Plan 12-01 captured PREVIEW_URL"
      via: "grep PREVIEW_URL .planning/phases/12-.../12-01-SUMMARY.md"
      pattern: "PREVIEW_URL value embedded in README"
---

<objective>
Document the three-tier branching/deploy model in `README.md` and embed the stable dev preview URL captured by Plan 12-01.

Purpose: Satisfy DEV-04 so contributors (and future-Kaleb) know which branch deploys where.
Output: A "Deployment" / "Branching & deployment" section in `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-create-dev-branch-for-live-preview-environment-separate-from/12-CONTEXT.md
@.planning/phases/12-create-dev-branch-for-live-preview-environment-separate-from/12-01-SUMMARY.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Branching & deployment" section to README.md</name>
  <files>README.md</files>
  <read_first>
    - `README.md` (full file — to choose the correct insertion point; the current "Deployment topology" section is around lines 86–119, and the "Environment variables" section is around lines 148–161 — insert the new section BETWEEN them, immediately after the "Services" section and before "Tech stack", OR after "Environment variables" if that flows better)
    - `.planning/phases/12-create-dev-branch-for-live-preview-environment-separate-from/12-01-SUMMARY.md` — extract the `PREVIEW_URL:` value. Run: `grep '^PREVIEW_URL:' .planning/phases/12-create-dev-branch-for-live-preview-environment-separate-from/12-01-SUMMARY.md | sed 's/^PREVIEW_URL:[[:space:]]*//'`
    - If that grep returns empty, STOP — Plan 12-01 did not deliver its required SUMMARY artifact. Do not invent a URL.
  </read_first>
  <action>
    Add a new section to `README.md` titled `## Branching & deployment` that documents the three-tier model (per D-04 / DEV-04). Insert it AFTER the "Services" table and BEFORE the "Tech stack" section (around line 132 in the current README). The section MUST contain:

    1. A short prose intro explaining the model.
    2. A markdown table with columns: Branch | Deploys to | URL | Notes
    3. Rows for: `main`, `dev`, feature branches (`feat/*` / `fix/*` / etc.).
    4. The captured `PREVIEW_URL` from Plan 12-01 in the `dev` row.

    Use this exact section template (substitute `<PREVIEW_URL>` with the value extracted from 12-01-SUMMARY.md, formatted as a clickable Markdown link `https://<PREVIEW_URL>`):

    ```markdown
    ## Branching & deployment

    Three-tier git workflow, each branch type maps to a distinct Vercel deployment:

    | Branch                 | Deploys to                              | URL                                                    | Notes                                                              |
    | ---------------------- | --------------------------------------- | ------------------------------------------------------ | ------------------------------------------------------------------ |
    | `main`                 | **Production**                          | https://kalebnim.dev · https://nim-kaleb.vercel.app    | Push to `main` ⇒ production deploy. Protected — merge via PR only. |
    | `dev`                  | **Stable preview** (long-lived)         | https://<PREVIEW_URL>                                  | Push to `dev` ⇒ auto-deploys to the same stable URL.               |
    | `feat/*`, `fix/*`, etc | **Ephemeral PR preview** (per branch)   | `nim-kaleb-git-<branch>-kaleb-nims-projects.vercel.app` | Each push gets a unique preview URL via Vercel Git Integration.    |

    **Working flow:**
    - Land production-ready work on `main` (via PR).
    - Use `dev` for shareable in-progress work and integration testing — no impact on production.
    - Use short-lived feature branches for isolated PR previews.

    **Environment variables** are scoped per environment in Vercel: Production vars live in the Production scope, `dev` and PR previews read from the Preview scope. See [Environment variables](#environment-variables) below for the keys each scope needs.
    ```

    Use the `Edit` tool to insert this block. Do NOT modify any other section of the README.

    Final sanity check after Edit: run `grep -c "main → production\|main.*Production" README.md` (must be ≥ 1) and `grep -c "$PREVIEW_URL" README.md` (must be ≥ 1) — where `$PREVIEW_URL` is the value from 12-01-SUMMARY.md.
  </action>
  <verify>
    <automated>PREVIEW_URL=$(grep '^PREVIEW_URL:' .planning/phases/12-create-dev-branch-for-live-preview-environment-separate-from/12-01-SUMMARY.md | sed 's/^PREVIEW_URL:[[:space:]]*//') &amp;&amp; grep -q "## Branching &amp; deployment" README.md &amp;&amp; grep -q "$PREVIEW_URL" README.md &amp;&amp; grep -q "Production" README.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Branching &amp; deployment$" README.md` returns exactly `1`
    - `grep -c "| \`main\`" README.md` returns ≥ `1` (main row exists)
    - `grep -c "| \`dev\`" README.md` returns ≥ `1` (dev row exists)
    - `grep -c "feat/\*\|feature branch\|PR preview" README.md` returns ≥ `1` (third tier documented)
    - The captured `PREVIEW_URL` value (from 12-01-SUMMARY.md) appears at least once in README.md
    - `grep -c "kalebnim.dev" README.md` returns ≥ `2` (existing references preserved + production row references it)
    - `git diff README.md` shows ONLY additions in the new section — no other lines modified
    - The README still renders as valid Markdown (no broken tables — verify table has consistent column count: every row in the new table has 4 pipes-delimited cells)
  </acceptance_criteria>
  <done>README.md contains a "Branching &amp; deployment" section documenting the three-tier model with the captured dev preview URL. Existing README content is preserved unchanged.</done>
</task>

</tasks>

<verification>
- `grep "## Branching & deployment" README.md` matches
- Captured `PREVIEW_URL` from 12-01-SUMMARY.md appears in README.md
- `git diff README.md` shows only additions (no deletions of existing content)
</verification>

<success_criteria>
- DEV-04: README documents three-tier branching/deploy model and lists the dev preview URL ✓
</success_criteria>

<output>
After completion, create `.planning/phases/12-create-dev-branch-for-live-preview-environment-separate-from/12-02-SUMMARY.md` noting the README section added, line range, and the embedded PREVIEW_URL value.
</output>
