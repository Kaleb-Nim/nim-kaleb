---
phase: 13-syai-meetups-page
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - app/page.tsx
  - public/meetups/.gitkeep
autonomous: false
requirements: [SYAI-01, SYAI-08, SYAI-09]
tags: [react, nextjs, routing, syai-meetups, verification]

must_haves:
  truths:
    - "Loading the site and clicking the SYAI Meetups directory row renders the real MeetupsPage component, not StubSectionPage."
    - "All other section routes (hackathons, sidequests, hobbies, links) still render StubSectionPage — no regression."
    - "Home page, floating mic, and voice overlay are unchanged."
    - "bun run build exits 0 with no new warnings or errors."
    - "public/meetups/ exists as a real directory so the kit's /meetups/<slug>-hero.jpg references at least resolve to a directory (404 on missing files is acceptable — the MeetupImage onError handler hides the broken image and the placeholder renders alongside since the source is non-null; per SYAI-04 a styled placeholder appears even on missing assets)."
  artifacts:
    - path: "app/page.tsx"
      provides: "Updated route dispatcher with syai-meetups branch"
      contains: "section.id === 'syai-meetups'"
    - path: "public/meetups/.gitkeep"
      provides: "Empty directory placeholder so Next.js can serve from /meetups/*"
      contains: ""
  key_links:
    - from: "app/page.tsx"
      to: "app/components/MeetupsPage.tsx"
      via: "default import + JSX usage in dispatcher branch"
      pattern: "import MeetupsPage"
    - from: "browser URL #/syai-meetups"
      to: "app/components/MeetupsPage.tsx render"
      via: "useHashRoute -> SECTIONS.find -> dispatcher branch"
      pattern: "syai-meetups"
---

<objective>
Wire the new `MeetupsPage` into the hash-route dispatcher in `app/page.tsx`,
create the empty `public/meetups/` directory so future asset drops are
servable from a stable path, and run a blocking human verification pass
against the locked Phase 13 acceptance criteria.

After this plan, `#/syai-meetups` renders the real meetups page. Plan 01 +
Plan 02 are complete prerequisites — this plan touches one production file and
adds an empty asset directory, then gates on visual + functional sign-off.

Purpose: Ship the Phase 13 deliverable. This is the only plan whose work is
visible to the user in the browser.

Output: One edit to `app/page.tsx` (add a third dispatcher branch alongside
`work-experience`), one empty `public/meetups/.gitkeep` so the asset directory
exists in git, and a human-verify checkpoint covering the SPEC's acceptance
criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-syai-meetups-page/13-01-PLAN.md
@.planning/phases/13-syai-meetups-page/13-02-PLAN.md
@app/page.tsx

<interfaces>
<!-- The dispatcher branch we need to ADD. -->

Current relevant fragment of `app/page.tsx` (lines 58-66):
```typescript
{isHome ? (
  <HomePage />
) : section ? (
  section.id === 'work-experience' ? (
    <WorkPage section={section} />
  ) : (
    <StubSectionPage section={section} />
  )
) : (
  <NotFoundPage />
)}
```

Replace the ternary chain with:
```typescript
{isHome ? (
  <HomePage />
) : section ? (
  section.id === 'work-experience' ? (
    <WorkPage section={section} />
  ) : section.id === 'syai-meetups' ? (
    <MeetupsPage section={section} />
  ) : (
    <StubSectionPage section={section} />
  )
) : (
  <NotFoundPage />
)}
```

The Phase 11 pattern (WorkPage dispatcher branch) is the template — extend it
with one more branch for `syai-meetups`, keeping `StubSectionPage` as the
final fallback so hackathons / sidequests / hobbies / links keep rendering
the Phase 10 stub.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire MeetupsPage into route dispatcher + create public/meetups directory</name>
  <read_first>
    - app/page.tsx (entire file — ~82 lines)
    - app/components/MeetupsPage.tsx (Plan 02 output — confirm export name + props)
    - app/lib/sections.ts (confirm 'syai-meetups' is still the section id after Plan 01)
  </read_first>
  <files>app/page.tsx, public/meetups/.gitkeep</files>
  <action>
    **Step 1 — Edit `app/page.tsx`:**

    Add `import MeetupsPage from './components/MeetupsPage';` to the imports
    block (insert directly after the `import WorkPage from './components/WorkPage';`
    line, around line 10, to keep imports grouped).

    Then replace the dispatcher ternary (currently lines 60-64) with the
    extended chain shown in the `<interfaces>` block above — add the
    `section.id === 'syai-meetups' ? <MeetupsPage section={section} /> : ...`
    branch between the existing `work-experience` branch and the
    `<StubSectionPage section={section} />` fallback.

    Do NOT touch:
    - Any other component (Starfield, Terminal, TerminalHeader, TerminalContent, FloatingMic, VoiceOverlay)
    - The `useRealtimeVoice` hook usage
    - The voice overlay open/close effects
    - The Esc keydown handler
    - The `useHashRoute` import or `route` derivation
    - The `isHome` / `section` derivation

    **Step 2 — Create `public/meetups/.gitkeep`:**

    Write an empty file at `public/meetups/.gitkeep`. This guarantees the
    directory exists in git (so Next.js's static file server has a real
    `public/meetups/` path to serve from) before any meetup image assets land.
    Per SYAI-04 acceptance: if the directory exists but a specific image is
    missing, `<MeetupImage>` renders the styled `[ no media ]` placeholder
    via the `onError` handler that hides the broken `<img>` (per
    `app/components/MeetupImage.tsx` from Plan 02 Task 1).

    Note: if `public/meetups/` already exists (perhaps the user dropped
    assets between Plan 01 and now), this is still safe — `.gitkeep` is
    inert and Next.js's public serving works either way.
  </action>
  <verify>
    <automated>bunx tsc --noEmit && bun run lint && grep -q "import MeetupsPage from './components/MeetupsPage'" app/page.tsx && grep -q "section.id === 'syai-meetups'" app/page.tsx && grep -q "<MeetupsPage section={section} />" app/page.tsx && [ -d public/meetups ] && bun run build > /tmp/syai-build.log 2>&1 && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import MeetupsPage from './components/MeetupsPage'" app/page.tsx` succeeds
    - `grep -q "section.id === 'syai-meetups'" app/page.tsx` succeeds
    - `grep -q "<MeetupsPage section={section} />" app/page.tsx` succeeds
    - `app/page.tsx` still contains the `WorkPage` branch (`grep -q "section.id === 'work-experience'" app/page.tsx`)
    - `app/page.tsx` still contains the `StubSectionPage` fallback (`grep -q "<StubSectionPage section={section} />" app/page.tsx`)
    - `[ -d public/meetups ]` returns true (directory exists)
    - `bunx tsc --noEmit` exits 0
    - `bun run lint` exits 0
    - `bun run build` exits 0 (full Next.js production build succeeds)
  </acceptance_criteria>
  <done>The dispatcher renders MeetupsPage for `#/syai-meetups`, other sections still stub-render, the build is green, and `public/meetups/` exists as a stable asset directory.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking" status="approved">
  <name>Task 2: Human verification — Phase 13 acceptance criteria [APPROVED 2026-05-19]</name>
  <what-built>
    Phase 13 wired end-to-end:
    - SYAI_ITEMS now contains 11 real meetups including the March 22 2025 SYAI x CYS Resume Roasting with Lim Mei Yu (Plan 01)
    - MeetupsPage + 5 supporting components render Layout B with hero/desc/speakers/gallery + lightbox (Plan 02)
    - Dispatcher routes `#/syai-meetups` to the real page; other section routes still stub (Plan 03 Task 1)
  </what-built>
  <how-to-verify>
    Pre-step: if `public/meetups/` is empty, every meetup will render the styled `[ no media ]` placeholder for hero + gallery. The page should STILL load and be visually coherent — this is acceptable for sign-off per SYAI-04 (placeholder, not broken-image icon). If real assets are placed in `public/meetups/` before this verification, they render in place of the placeholders.

    Run `bun dev` and open `http://localhost:3000` in a browser. Then:

    **1. Home unchanged (SYAI-09):**
       - Page loads at `/` and renders the directory home with the 6 section rows and the floating mic — no regression vs. Phase 10/11 behaviour.

    **2. Navigation (SYAI-01):**
       - Click the "syai-meetups" directory row OR navigate directly to `http://localhost:3000/#/syai-meetups`.
       - The page renders 11 meetup cards, NOT the "this section is being finalised" stub.
       - Page header shows `← ~/kaleb / syai-meetups`, title `./syai-meetups — talks I hosted at SYAI`, the intro line, and the gold `[11 entries]` chip.

    **3. Layout B (SYAI-04):**
       - Each card shows: top ribbon `[#NN]` gold chip + date + title; then a 2-column row with hero on the LEFT and description on the RIGHT; then the SPEAKERS [N] block full-width; then a row of square gallery thumbs.
       - Cards with no hero asset show the `[ no media ]` placeholder grid with `hero photo` label — no broken-image icon, no console errors.
       - The March 22 2025 entry shows the SpeakersBlock chassis with at least one row (real speaker name OR placeholder `TBD` — both acceptable; full speaker rosters land in a future content-population phase).
       - The March 2025 entry shows a gold `SIGN UP ↗` chip linking to `https://forms.gle/FpKePiMijNLDtudV6` (this is the ONLY event with a confirmed signup link; other entries should not show the chip).

    **3b. Asset serving (SYAI-07):**
       - Drop ANY test image (e.g. `cp public/work-logos/raid.png public/meetups/syai-2025-03-resume-roasting-hero.jpg`) into `public/meetups/` matching one of the referenced paths in SYAI_ITEMS.
       - Reload the meetups page.
       - Open DevTools Network tab — confirm the request for that file returns **HTTP 200** (not 404).
       - Open DevTools Console — confirm there are **zero error-level entries** related to missing meetup assets (404 warnings on still-missing files are acceptable; uncaught JS errors are NOT).
       - Delete the test file when done.
       - Without the test file present (clean state), the placeholder renders cleanly and the console contains no uncaught errors (only benign 404s).

    **4. Lightbox — click (SYAI-05):**
       - Click any non-placeholder hero or thumb (or, if `public/meetups/` is empty, temporarily drop ANY `.jpg` into `public/meetups/<expected-name>.jpg` and refresh — the slot will become clickable).
       - The lightbox opens, dims the background, shows the image enlarged, and renders the caption `#NN · <title> (<date>)` plus `(idx / total · ← → to navigate · Esc to close)`.
       - Pressing `Esc` closes the lightbox.
       - Clicking the dimmed backdrop (away from the image) closes the lightbox.
       - Clicking the inner image itself does NOT close the lightbox (verifies the `e.stopPropagation()` from Plan 02).
       - Pressing `ArrowRight` advances to the next image in the flat list; `ArrowLeft` goes back; both wrap around.
       - Clicking the gold `✕` button closes the lightbox.

    **5. Lightbox — touch (SYAI-05):**
       - Resize the browser to ~375px (or use device emulation) and tap a non-placeholder image.
       - The lightbox opens and is dismissible by tapping the backdrop.
       - Tapping the inner image does NOT close.

    **6. Speakers (SYAI-06):**
       - Every meetup card shows the SPEAKERS block with at least one row.
       - Each row has `<name>` in green, `│` divider, role in dim green, and a gold `in ↗` chip linking to `speaker.linkedin`.
       - Clicking the `in ↗` chip opens the LinkedIn URL in a new tab (`target="_blank"`).

    **7. Other section routes unchanged (SYAI-01 + SYAI-09):**
       - Visit `#/hackathons`, `#/sidequests`, `#/hobbies`, `#/links` — each renders the existing Phase 10 stub ("this section is being finalised — check back soon."), NOT the meetups layout.
       - Visit `#/work-experience` — renders the Phase 11 work timeline as before.
       - Visit a bogus hash like `#/nonexistent` — renders the red 404 page.

    **8. Mobile holds at 360px (SYAI-08):**
       - In devtools, set viewport to 360x800.
       - Scroll the meetups page top-to-bottom — no horizontal scrollbar appears.
       - The 2-col hero/desc grid can stack or remain 2-col (kit-as-shipped); the gallery thumb grid stays inside the card; the speakers list does not overflow.

    **9. Desktop holds at 1024px+ (SYAI-08):**
       - Set viewport to 1280x800.
       - The meetup card body fits within the 860px terminal max-width; no overflow.

    **10. Voice overlay unchanged (SYAI-09):**
       - On the meetups page, tap the floating `● talk to me` mic.
       - The voice overlay opens.
       - Press `Esc` — the overlay closes cleanly.
       - Re-open it — no audio overlap / no console errors.

    **11. Locked SPEC negatives (SYAI-08):**
       - NO Tweaks panel appears anywhere on the meetups page.
       - NO filter / search input is visible.
       - NO "Sign up for SYAI" page-level CTA appears (the per-event `SIGN UP ↗` chip on the March 2025 card is the only signup affordance, and it's event-level not page-level).
       - NO new URL routes have appeared (lightbox is overlay-only — the hash stays `#/syai-meetups` when it opens).

    **12. Production build (SYAI-08):**
       - Confirm `bun run build` exited 0 in Task 1's automated verify; if not, re-run and report any error.

    Report any FAIL with a screenshot or copy of the console error.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 13 items pass, OR list the failing item numbers + observations (e.g. "3 fails — March 2025 card missing the Lim Mei Yu row" or "5 fails — clicking inner image closes lightbox").
  </resume-signal>
</task>

</tasks>

<verification>
After both tasks complete:
- `bun run build` exits 0
- `bunx tsc --noEmit` exits 0
- `app/page.tsx` has three dispatcher branches: home, work-experience, syai-meetups (+ stub fallback + 404)
- `public/meetups/` exists in git (via the `.gitkeep` placeholder)
- Human checkpoint returns "approved"
</verification>

<success_criteria>
- `#/syai-meetups` route renders MeetupsPage; no other section route is affected
- All 12 manual verification items pass
- No new TypeScript / lint / build errors
- `public/meetups/` exists in git so Next.js's static asset path is stable for future drops
- Phase 13 acceptance criteria from SYAI-01..09 are satisfied (SYAI-02..07 from Plans 01 + 02; SYAI-01, SYAI-08, SYAI-09 from this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/13-syai-meetups-page/13-03-SUMMARY.md` describing:
- The exact edit applied to `app/page.tsx` (single new import + one new ternary branch)
- The state of `public/meetups/` at sign-off (empty / partially populated / fully populated — and which slugs are still missing assets, derived from Plan 01 SUMMARY's path list)
- Which of the 12 human-verify items passed and any caveats (e.g. "asset drop pending — verified with synthetic test image")
- Any follow-up items pulled out into the todos queue (asset drops, speaker name backfill, etc.)
</output>
