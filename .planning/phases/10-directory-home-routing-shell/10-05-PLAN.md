---
phase: 10-directory-home-routing-shell
plan: 05
type: execute
wave: 2
depends_on: [10-02]
files_modified:
  - app/components/PageHeader.tsx
  - app/components/StubSectionPage.tsx
  - app/components/NotFoundPage.tsx
autonomous: true
requirements: [ROUTE-01, ROUTE-03]
must_haves:
  truths:
    - "PageHeader renders breadcrumb ← ~/kaleb / section.path + section.title + section.intro + count chip"
    - "StubSectionPage renders PageHeader + 'being finalised' line + FooterMeta link back to '#/'"
    - "NotFoundPage renders red [SYSTEM] 404 line + gold link back to '#/'"
  artifacts:
    - path: "app/components/PageHeader.tsx"
      provides: "Reused header for section pages (and Phase 11 WorkPage)"
      exports: ["default PageHeader", "FooterMeta"]
    - path: "app/components/StubSectionPage.tsx"
      provides: "Coming-soon stub for sections not yet built"
      exports: ["default StubSectionPage"]
    - path: "app/components/NotFoundPage.tsx"
      provides: "404 page"
      exports: ["default NotFoundPage"]
---

<objective>
Port `PageHeader` + `FooterMeta` from `pages.jsx` to TSX, plus a new `StubSectionPage` (renders PageHeader + "being finalised" body + FooterMeta) and a `NotFoundPage` (red [SYSTEM] 404 line + gold "← back to ~/kaleb" link). All three are consumed by Plan 10-08's route dispatch.

Purpose: Five of the six sections + the unknown-route fallback all need landing pages NOW (Phase 11 only replaces the work-experience one).
Output: 3 new component files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/v3-design-kit/pages.jsx
@.planning/research/v3-design-kit/index.html
@app/lib/sections.ts

<interfaces>
From app/lib/sections.ts:
```ts
export interface Section { id: string; path: string; count: number | string; title: string; intro: string; footer?: string; /* ... */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create app/components/PageHeader.tsx (PageHeader + FooterMeta)</name>
  <read_first>
    - .planning/research/v3-design-kit/pages.jsx (lines 18-86 for PageHeader, lines 635-649 for FooterMeta)
    - app/lib/sections.ts (Section type)
  </read_first>
  <files>app/components/PageHeader.tsx</files>
  <action>
    Create `app/components/PageHeader.tsx` with `'use client'` (so it can be rendered inside the client-only route tree).

    Imports:
    ```ts
    'use client';
    import type { Section } from '@/app/lib/sections';
    ```

    Define `pgStyles` constant verbatim (pages.jsx lines 6-15): green '#00FF00', greenDim 'rgba(0,255,0,0.78)', greenMute 'rgba(0,255,0,0.55)', greenFaint 'rgba(0,255,0,0.38)', gold '#FFD700', red '#FF4444', glow '0 0 4px rgba(0,255,0,0.4), 0 0 8px rgba(0,255,0,0.2)', goldGlow '0 0 4px rgba(255,215,0,0.45), 0 0 8px rgba(255,215,0,0.22)'.

    Default-export `PageHeader({ section }: { section: Section })`. Body renders three blocks (verbatim from pages.jsx lines 18-86):

    1. Breadcrumb `<div>` containing:
       - `<a href="#/" className="kni-crumb-home" style={{ color: '#FFD700', textShadow: pgStyles.goldGlow, textDecoration: 'none', fontWeight: 700, padding: '4px 8px', border: '1px solid rgba(255,215,0,0.45)', borderRadius: 4 }}>← ~/kaleb</a>`
       - `<span style={{ color: pgStyles.greenFaint }}>/</span>`
       - `<span style={{ color: pgStyles.green, textShadow: pgStyles.glow, fontWeight: 700 }}>{section.path}</span>`
       Parent `<div>` styles: `display: 'flex', alignItems: 'center', gap: 6, fontSize: 'clamp(0.74rem, 1.8vw, 0.8rem)', color: pgStyles.greenFaint, textShadow: 'none', marginBottom: 14, flexWrap: 'wrap'`.

    2. Title `<div>` with `color: pgStyles.green, fontSize: 'clamp(1.15rem, 4vw, 1.5rem)', fontWeight: 700, textShadow: '0 0 6px rgba(0,255,0,0.6), 0 0 14px rgba(0,255,0,0.3)', lineHeight: 1.25` rendering `{section.title}`. Followed by an optional intro `<div>` (when `section.intro`) with `color: pgStyles.greenDim, fontSize: 'clamp(0.78rem, 2vw, 0.86rem)', textShadow: 'none', marginTop: 6, lineHeight: 1.55, maxWidth: 620`.

    3. Count chip `<div>` with `display: 'inline-flex', alignItems: 'center', gap: 8, marginTop: 12, color: pgStyles.gold, textShadow: pgStyles.goldGlow, fontSize: '0.76rem', fontWeight: 700, letterSpacing: '0.06em', border: '1px solid rgba(255,215,0,0.4)', padding: '3px 10px'` rendering `[{section.count} entries]`.

    4. Divider `<div>` with `height: 1, margin: '16px 0', background: 'linear-gradient(90deg, rgba(0,255,0,0.45), rgba(0,255,0,0.05))'`.

    Wrap all four inside a root `<div>` with `fontFamily: '"Anonymous Pro", monospace'`.

    Also export named `FooterMeta({ section }: { section: Section })` — port pages.jsx lines 635-649:
    ```tsx
    <div style={{
      fontFamily: '"Anonymous Pro", monospace',
      color: pgStyles.greenFaint, textShadow: 'none',
      fontSize: '0.72rem', marginTop: 18, letterSpacing: '0.04em',
    }}>
      {section.footer || `[${section.count} entries]`} · ‹ tap{' '}
      <a href="#/" style={{ color: pgStyles.gold, textShadow: pgStyles.goldGlow, textDecoration: 'underline' }}>~/kaleb</a> to return home
    </div>
    ```
  </action>
  <verify>
    <automated>grep -c "export default function PageHeader" app/components/PageHeader.tsx | grep -q "^1$" && grep -c "export function FooterMeta" app/components/PageHeader.tsx | grep -q "^1$" && grep -c "← ~/kaleb" app/components/PageHeader.tsx | grep -q "^1$" && grep -c "className=\"kni-crumb-home\"" app/components/PageHeader.tsx | grep -q "^1$" && grep -c "from '@/app/lib/sections'" app/components/PageHeader.tsx | grep -q "^1$" && bunx tsc --noEmit 2>&1 | grep -E "PageHeader\.tsx" | head -5</automated>
  </verify>
  <done>PageHeader + FooterMeta exported; types resolve; breadcrumb anchor uses `href="#/"`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create app/components/StubSectionPage.tsx</name>
  <read_first>
    - app/components/PageHeader.tsx (output of Task 1)
    - .planning/phases/10-directory-home-routing-shell/10-CONTEXT.md (specifics — "Stub section page should render PageHeader + short 'this section is being finalised — check back soon' message + FooterMeta. Stay on-brand. Direct visitors back to home.")
  </read_first>
  <files>app/components/StubSectionPage.tsx</files>
  <action>
    Create `app/components/StubSectionPage.tsx` with `'use client'`.

    Imports:
    ```ts
    'use client';
    import type { Section } from '@/app/lib/sections';
    import PageHeader, { FooterMeta } from './PageHeader';
    ```

    Default-export:
    ```tsx
    export default function StubSectionPage({ section }: { section: Section }) {
      return (
        <div className="kni-page">
          <PageHeader section={section} />
          <div style={{
            color: 'rgba(0,255,0,0.78)',
            fontSize: 'clamp(0.82rem, 2vw, 0.9rem)',
            lineHeight: 1.7,
            textShadow: '0 0 4px rgba(0,255,0,0.3), 0 0 8px rgba(0,255,0,0.15)',
            padding: '12px 0',
          }}>
            {'> '}this section is being finalised — check back soon.
          </div>
          <div style={{
            color: 'rgba(0,255,0,0.55)',
            fontSize: '0.78rem',
            textShadow: 'none',
            marginTop: 4,
          }}>
            {'  '}in the meantime, ask the voice clone about{' '}
            <span style={{ color: '#FFD700', textShadow: '0 0 4px rgba(255,215,0,0.4)' }}>{section.path}</span>
            {' '}or tap{' '}
            <a href="#/" style={{ color: '#FFD700', textShadow: '0 0 4px rgba(255,215,0,0.4)', textDecoration: 'underline' }}>~/kaleb</a>
            {' '}to head home.
          </div>
          <FooterMeta section={section} />
        </div>
      );
    }
    ```

    Note: wrap in `<div className="kni-page">` so the kniPageIn animation fires on route changes.
  </action>
  <verify>
    <automated>grep -c "export default function StubSectionPage" app/components/StubSectionPage.tsx | grep -q "^1$" && grep -c "being finalised" app/components/StubSectionPage.tsx | grep -q "^1$" && grep -c "className=\"kni-page\"" app/components/StubSectionPage.tsx | grep -q "^1$" && grep -c "import PageHeader, { FooterMeta }" app/components/StubSectionPage.tsx | grep -q "^1$" && bunx tsc --noEmit 2>&1 | grep -E "StubSectionPage" | head -5</automated>
  </verify>
  <done>File compiles; renders PageHeader + stub body + FooterMeta; back-home link present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create app/components/NotFoundPage.tsx</name>
  <read_first>
    - .planning/research/v3-design-kit/index.html (lines 259-278 — NotFoundPage component)
  </read_first>
  <files>app/components/NotFoundPage.tsx</files>
  <action>
    Create `app/components/NotFoundPage.tsx` with `'use client'`.

    Default-export a function with no props. Port index.html lines 259-278 verbatim:

    ```tsx
    'use client';

    export default function NotFoundPage() {
      return (
        <div className="kni-page">
          <div style={{
            color: '#FF4444',
            textShadow: '0 0 6px rgba(255,68,68,0.6)',
            fontWeight: 700,
            fontSize: '1.1rem',
          }}>
            [SYSTEM] 404 — directory not found
          </div>
          <div style={{
            color: 'rgba(0,255,0,0.7)',
            marginTop: 6,
            fontSize: '0.82rem',
          }}>
            That path doesn&apos;t exist.{' '}
            <a href="#/" style={{
              color: '#FFD700',
              textShadow: '0 0 4px rgba(255,215,0,0.4)',
              textDecoration: 'underline',
            }}>← back to ~/kaleb</a>
          </div>
        </div>
      );
    }
    ```
  </action>
  <verify>
    <automated>grep -c "export default function NotFoundPage" app/components/NotFoundPage.tsx | grep -q "^1$" && grep -c "\[SYSTEM\] 404" app/components/NotFoundPage.tsx | grep -q "^1$" && grep -c "← back to ~/kaleb" app/components/NotFoundPage.tsx | grep -q "^1$" && bunx tsc --noEmit 2>&1 | grep -E "NotFoundPage" | head -5</automated>
  </verify>
  <done>File compiles; red 404 line + gold back-home link present.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit` clean for all three files
- grep confirms ROUTE-01 requirement strings exist verbatim ("being finalised", "[SYSTEM] 404")
- All three exports importable from `@/app/components/...`
</verification>

<success_criteria>
- PageHeader + FooterMeta reusable (Phase 11 will reuse PageHeader for WorkPage)
- StubSectionPage works for any of the 5 non-work sections (and the work stub until Phase 11)
- NotFoundPage covers unknown routes
</success_criteria>

<output>
After completion, create `.planning/phases/10-directory-home-routing-shell/10-05-SUMMARY.md`.
</output>
