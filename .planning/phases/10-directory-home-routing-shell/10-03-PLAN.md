---
phase: 10-directory-home-routing-shell
plan: 03
type: execute
wave: 2
depends_on: [10-02]
files_modified:
  - app/components/Directory.tsx
autonomous: true
requirements: [HOME-04]
must_haves:
  truths:
    - "Directory renders a list of clickable rows, one per section"
    - "Each row is at least 60px tall (touch-friendly)"
    - "Each row is an <a href='#/{section.id}'> so middle-click/right-click works and back/forward updates state"
    - "Pressed state changes background + border-left + chevron transform via pointer events"
  artifacts:
    - path: "app/components/Directory.tsx"
      provides: "<Directory rows={SECTIONS} onNav={fn} /> and named <DirRow /> export"
      exports: ["default Directory", "DirRow"]
  key_links:
    - from: "Directory.tsx"
      to: "app/lib/sections.ts Section type"
      via: "import"
      pattern: "from '@/app/lib/sections'"
---

<objective>
Port `.planning/research/v3-design-kit/Directory.jsx` to TSX at `app/components/Directory.tsx`. Each row is an `<a href="#/{id}">` whose click also invokes the optional `onNav` callback (which calls `navigateTo` from `useHashRoute.ts`). Inline styles are preserved verbatim.

Purpose: HomePage (Plan 10-06) renders this with `<Directory rows={SECTIONS} onNav={navigateTo} />`.
Output: `app/components/Directory.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/v3-design-kit/Directory.jsx
@app/lib/sections.ts

<interfaces>
From app/lib/sections.ts (created in Plan 10-02):
```ts
export interface Section {
  id: string;
  path: string;
  count: number | string;
  desc: string;
  // ...other fields
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Port Directory.jsx → app/components/Directory.tsx</name>
  <read_first>
    - .planning/research/v3-design-kit/Directory.jsx (full file — source of every literal style value)
    - app/lib/sections.ts (Section type import)
  </read_first>
  <files>app/components/Directory.tsx</files>
  <action>
    Create `app/components/Directory.tsx` with `'use client'` (uses React state for pressed).

    Imports:
    ```ts
    'use client';
    import { useState } from 'react';
    import type { Section } from '@/app/lib/sections';
    ```

    Define `dirStyles` constant verbatim (Directory.jsx lines 5-14): green '#00FF00', greenDim 'rgba(0,255,0,0.78)', greenMute 'rgba(0,255,0,0.55)', greenFaint 'rgba(0,255,0,0.38)', gold '#FFD700', red '#FF4444', glow '0 0 4px rgba(0,255,0,0.45), 0 0 8px rgba(0,255,0,0.22)', goldGlow '0 0 4px rgba(255,215,0,0.45), 0 0 8px rgba(255,215,0,0.22)'.

    Export named `DirRow` component with props:
    ```ts
    interface DirRowProps { row: Section; onNav?: (id: string) => void; }
    export function DirRow({ row, onNav }: DirRowProps) { ... }
    ```

    Inside `DirRow`:
    - `const [pressed, setPressed] = useState(false);`
    - Returns a single `<a>` with:
      - `href={`#/${row.id}`}`
      - `onClick={(e) => { e.preventDefault(); onNav?.(row.id); }`
      - `onPointerDown={() => setPressed(true)}`, `onPointerUp={() => setPressed(false)}`, `onPointerLeave={() => setPressed(false)}`
      - `className="kni-dir-row"`
      - The exact inline `style` object from Directory.jsx lines 31-54 (grid template, columnGap, padding, minHeight 60, background pressed/idle ternary, borderTop, borderLeft pressed/idle ternary, boxShadow ternary, transition, transform ternary, cursor pointer, WebkitTapHighlightColor 'transparent', fontFamily '"Anonymous Pro", monospace')

    Inside the `<a>`, render four spans EXACTLY as in lines 57-112 of Directory.jsx:
    1. Leading marker span containing `<span className="kni-dir-icon">▸</span>` then `<span style={{ color: dirStyles.green, textShadow: dirStyles.glow, fontWeight: 700 }}>./{row.path}</span>` then `<span style={{ color: dirStyles.greenFaint, fontWeight: 400 }}>/</span>`
    2. Description span with `className="kni-dir-desc"` rendering `— {row.desc}`
    3. Count badge: `<span>[{row.count}]</span>` with gold styles
    4. Right chevron `›` with `aria-hidden="true"` and pressed-conditional color/transform

    Default-export `Directory` component:
    ```ts
    interface DirectoryProps { rows: Section[]; onNav?: (id: string) => void; }
    export default function Directory({ rows, onNav }: DirectoryProps) { ... }
    ```

    Renders the `ls` header (lines 119-130 of source) — `<div>$ ls ~/kaleb</div>` and `<div>{rows.length} dirs · tap any to open</div>` — then a wrapper `<div className="kni-dir-list">` with `borderBottom: '1px solid rgba(0,255,0,0.18)', background: '#000'`, then `{rows.map((row) => <DirRow key={row.id} row={row} onNav={onNav} />)}`.

    DO NOT use `next/link` here — the design depends on `<a href="#/...">` so right-click/middle-click open in new tab semantics work correctly and hashchange fires. The `e.preventDefault()` + `onNav` path is only for left-click.
  </action>
  <verify>
    <automated>grep -c "export default function Directory" app/components/Directory.tsx | grep -q "^1$" && grep -c "export function DirRow" app/components/Directory.tsx | grep -q "^1$" && grep -c "className=\"kni-dir-row\"" app/components/Directory.tsx | grep -q "^1$" && grep -c "minHeight: 60" app/components/Directory.tsx | grep -q "^1$" && grep -c "href={\`#/" app/components/Directory.tsx | grep -q "^1$" && grep -c "ls ~/kaleb" app/components/Directory.tsx | grep -q "^1$" && grep -c "from '@/app/lib/sections'" app/components/Directory.tsx | grep -q "^1$" && bunx tsc --noEmit 2>&1 | grep -E "Directory\.tsx" | head -5</automated>
  </verify>
  <done>
    File compiles; renders one `<a>` per row; pressed state toggles; `<a>` href is `#/{id}`; `onNav` invoked on left-click after preventDefault.
  </done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit` clean for Directory.tsx
- Visual smoke: import into a sandbox page and confirm 6 rows render, each ≥60px tall (verified visually by Plan 10-08 mounting it on `/`)
</verification>

<success_criteria>
- DirRow + Directory exports present
- Inline styles match design kit literals (background ternaries, border ternaries, transform ternary)
- minHeight 60 ensures HOME-04 mobile tap target
- Touches no other files
</success_criteria>

<output>
After completion, create `.planning/phases/10-directory-home-routing-shell/10-03-SUMMARY.md`.
</output>
