---
phase: 11-work-experience-page
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - app/page.tsx
autonomous: false
requirements: [WORK-06]
tags: [react, nextjs, routing, work-experience]

must_haves:
  truths:
    - "Visiting #/work-experience renders <WorkPage /> instead of <StubSectionPage />."
    - "FooterMeta at the bottom shows '[4 entries] · most recent first · ‹ tap ~/kaleb to return home'."
    - "All other hash routes (#/syai-meetups, #/hackathons, #/sidequests, #/hobbies, #/links) still render the Phase 10 stub — no regressions."
    - "The home route (#) and unknown routes (#/asdf) are unaffected."
    - "`bun run build` succeeds with zero new errors."
    - "Logo PNGs at /work-logos/{raid,tensorplex,artc}.png are reachable on the dev server (HTTP 200)."
  artifacts:
    - path: "app/page.tsx"
      provides: "Hash-route dispatcher that selects WorkPage for the work-experience slug"
      contains: "section.id === 'work-experience'"
  key_links:
    - from: "app/page.tsx dispatcher"
      to: "app/components/WorkPage.tsx"
      via: "default import + conditional render"
      pattern: "import WorkPage.*from.*WorkPage"
    - from: "app/page.tsx"
      to: "section.id === 'work-experience'"
      via: "branch condition before falling through to StubSectionPage"
      pattern: "work-experience"
---

<objective>
Wire the new `WorkPage` component (built in Plan 01) into the hash-route dispatcher at `app/page.tsx`. When `section.id === 'work-experience'`, render `<WorkPage section={section} />`; otherwise keep the existing `<StubSectionPage section={section} />` behaviour. After this plan, the `#/work-experience` route shows the full 4-role timeline and a human-verifies it visually at 360px and 1024px+.

Purpose: Close out Phase 11 by activating the WorkPage on its route and confirming there are no regressions to home, stubs, or the floating-mic / voice-overlay flow.

Output: One edit to `app/page.tsx` and a human-verification checkpoint that records the visual outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-work-experience-page/11-CONTEXT.md
@.planning/phases/11-work-experience-page/11-01-SUMMARY.md
@.planning/phases/10-directory-home-routing-shell/10-CONTEXT.md
@app/page.tsx
@app/components/StubSectionPage.tsx

<interfaces>
<!-- From Plan 01 (already built when this plan runs) -->

`app/components/WorkPage.tsx`:
```typescript
export default function WorkPage({ section }: { section: Section }): JSX.Element;
```

Current `app/page.tsx` dispatcher block (lines 50-63):
```typescript
const isHome = route === '';
const section = isHome ? null : SECTIONS.find((s) => s.id === route);

return (
  <>
    <Starfield />
    <Terminal>
      <TerminalHeader />
      <TerminalContent>
        {isHome ? (
          <HomePage />
        ) : section ? (
          <StubSectionPage section={section} />   // ← replace with WorkPage when slug matches
        ) : (
          <NotFoundPage />
        )}
      </TerminalContent>
    </Terminal>
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire WorkPage into the hash-route dispatcher</name>
  <read_first>
    - app/page.tsx (current dispatcher, lines 1-76)
    - app/components/WorkPage.tsx (from Plan 01 — verify the default export)
    - .planning/phases/10-directory-home-routing-shell/10-CONTEXT.md (Phase 10 routing decisions — confirm the section-resolution pattern stays intact)
  </read_first>
  <files>app/page.tsx</files>
  <action>
    Edit `app/page.tsx`:

    1. Add an import after the existing `StubSectionPage` import:
       ```typescript
       import WorkPage from './components/WorkPage';
       ```

    2. Replace the existing render branch:
       ```typescript
       ) : section ? (
         <StubSectionPage section={section} />
       ) : (
       ```
       with:
       ```typescript
       ) : section ? (
         section.id === 'work-experience' ? (
           <WorkPage section={section} />
         ) : (
           <StubSectionPage section={section} />
         )
       ) : (
       ```

    Do NOT:
    - Touch the voice overlay wiring, FloatingMic, useRealtimeVoice, or useHashRoute usage
    - Modify the Starfield / Terminal / TerminalHeader / TerminalContent chrome
    - Remove the `StubSectionPage` import — it still renders the other 5 sections (syai-meetups, hackathons, sidequests, hobbies, links)
    - Add new state, effects, or hooks

    Per D-Implementation Decisions (LOCKED) > "Route wiring": "when `section.id === 'work-experience'`, render `<WorkPage section={section} />` instead of `<StubSectionPage section={section} />`. Keep stub for all other sections."
  </action>
  <verify>
    <automated>bunx tsc --noEmit && grep -q "import WorkPage from './components/WorkPage'" app/page.tsx && grep -q "section.id === 'work-experience'" app/page.tsx && grep -q "StubSectionPage" app/page.tsx && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import WorkPage from './components/WorkPage'" app/page.tsx` succeeds
    - `grep -q "section.id === 'work-experience'" app/page.tsx` succeeds
    - `grep -q "<WorkPage section={section}" app/page.tsx` succeeds
    - `grep -q "<StubSectionPage section={section}" app/page.tsx` succeeds (stub still rendered for other sections)
    - `bunx tsc --noEmit` exits 0
    - `bun run build` exits 0
  </acceptance_criteria>
  <done>The dispatcher conditionally renders WorkPage on the work-experience route, every other route still uses its Phase 10 component, and the production build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — visual + responsive check</name>
  <what-built>
    `WorkPage` now renders at `#/work-experience`. The page shows the PageHeader (breadcrumb · gold `← ~/kaleb` chip · path · title · intro · gold `[4 entries]` chip), a vertical phosphor-green rail with 4 status nodes (◉ ACTIVE, ● SHIPPED, ● SHIPPED, ○ ARCHIVED top-to-bottom), each entry has a square logo chip (raid white-bg, tensorplex cream `#F4F1EB`-bg, artc ×2 white-bg) followed by date / title / `[TAG]` chip / org / note. FooterMeta at the bottom reads `[4 entries] · most recent first · ‹ tap ~/kaleb to return home`.
  </what-built>
  <how-to-verify>
    Run the dev server and step through these checks. Report any deviation from the pixel-faithful contract in `.planning/research/v3-design-kit/pages.jsx`.

    Setup:
    1. Run `bun dev` (or confirm it is already running on http://localhost:3000)
    2. Open http://localhost:3000 — confirm home page renders unchanged (HomePage with directory listing)

    Work-experience route checks (1024px+ desktop window):
    3. Navigate to http://localhost:3000/#/work-experience
    4. Confirm breadcrumb shows `← ~/kaleb / work-experience` with the gold chip on `← ~/kaleb`
    5. Confirm title reads `./work-experience — paid AI engineering` in phosphor green
    6. Confirm intro line: `Roles where someone gave me money to ship AI systems. Newest first.`
    7. Confirm gold `[4 entries]` chip below intro
    8. Confirm a vertical green rail on the left with 4 round status nodes
    9. Confirm entry order top-to-bottom: RAiD (◉ ACTIVE) → Tensorplex Labs (● SHIPPED) → ARTC AI Engineer (● SHIPPED) → ARTC Dev Sci Intern (○ ARCHIVED)
    10. Confirm each entry has its logo chip visible (RAiD white bg, Tensorplex cream `#F4F1EB` bg, ARTC ×2 white bg). All three PNGs should load — no broken-image icons.
    11. Open DevTools Network tab → reload → confirm `/work-logos/raid.png`, `/work-logos/tensorplex.png`, `/work-logos/artc.png` (×2 reuses cache) return HTTP 200
    12. Confirm footer reads `[4 entries] · most recent first · ‹ tap ~/kaleb to return home` and the `~/kaleb` link returns to the home page when clicked

    Mobile / 360px check:
    13. DevTools → device toolbar → iPhone SE or set width to 360px
    14. Confirm no horizontal scroll
    15. Confirm logo chip + content stack legibly (chip ≈56px, title wraps if needed, no overflow)
    16. Confirm the rail and status nodes still align with the title baseline of each entry

    Regression checks:
    17. Visit `#/syai-meetups`, `#/hackathons`, `#/sidequests`, `#/hobbies`, `#/links` — each should still render the Phase 10 stub (NOT the work page)
    18. Visit `#/does-not-exist` — should render NotFoundPage
    19. Click the floating mic — VoiceOverlay should open; press Esc — should close. No JS errors in console.
    20. Return to home (`#/`) — directory should render as in Phase 10.

    Reduced-motion check (if convenient):
    21. macOS System Settings → Accessibility → Display → Reduce motion (or DevTools "Emulate CSS prefers-reduced-motion") — confirm the page still renders cleanly (no broken animations).
  </how-to-verify>
  <resume-signal>Type "approved" if all 21 checks pass. Otherwise list the failing check number(s) and the observed deviation (screenshot ideal) so the planner can scope a fix plan.</resume-signal>
  <done>User signs off that #/work-experience renders the pixel-faithful 4-role timeline, all logos load, responsive behaviour holds at 360px, and no regressions exist on home / other stubs / voice overlay.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit` and `bun run build` both exit 0
- `app/page.tsx` imports WorkPage and conditionally renders it for `section.id === 'work-experience'`
- All other sections still render `StubSectionPage`
- Human verification checklist signed off
- `public/work-logos/{raid,tensorplex,artc}.png` reachable from the dev server
</verification>

<success_criteria>
- WORK-06: FooterMeta displays `[4 entries] · most recent first · ‹ tap ~/kaleb to return home` and the back-link works
- WORK-01..05 visually confirmed by human (already wired in code by Plan 01; verified for real here)
- No regressions on home, other stubs, NotFoundPage, FloatingMic, VoiceOverlay
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/11-work-experience-page/11-02-SUMMARY.md` describing:
- The single-line dispatcher edit in `app/page.tsx`
- Human-verification outcome (which checks passed, any deviations)
- Confirmation that `bun run build` exits 0
- Any follow-ups raised during visual review (deferred to a future phase if cosmetic, or noted as a gap if blocking)
</output>
