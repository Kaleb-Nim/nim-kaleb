---
phase: 16-voice-cta-visibility-make-talk-to-me-unmissable
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - app/components/FloatingMic.tsx
autonomous: false
requirements:
  - VOICE-VIS-02
  - VOICE-VIS-03

must_haves:
  truths:
    - "FloatingMic is visually more prominent than before — idle glow animation draws the eye"
    - "Mobile FloatingMic shows short label 'voice' instead of collapsing to bare dot"
    - "First-time visitors see a tooltip above FloatingMic that auto-dismisses after 6 seconds"
    - "The tooltip does not reappear on subsequent visits or route changes"
  artifacts:
    - path: "app/components/FloatingMic.tsx"
      provides: "Improved FloatingMic with idle glow, mobile label, and first-visit tooltip"
  key_links:
    - from: "app/components/FloatingMic.tsx"
      to: "localStorage"
      via: "kni-voice-nudge-seen key (shared with VoiceCTA)"
      pattern: "localStorage.*kni-voice-nudge-seen"
    - from: "app/components/FloatingMic.tsx"
      to: "app/globals.css"
      via: "kniMicIdle keyframe (added in Plan 01)"
      pattern: "kniMicIdle"
---

<objective>
Improve FloatingMic visual presence with an idle glow animation, better mobile sizing (short "voice" label instead of bare dot), a subtle gold background tint, increased border width, and a first-visit tooltip that auto-dismisses after 6 seconds. The tooltip shares the same localStorage key as VoiceCTA's nudge so the nudge fires exactly once across both components.

Purpose: The FloatingMic button currently blends into the terminal aesthetic — gold on black with no idle animation. Visitors on mobile see only a tiny dot. These changes make the button unmissable without being obnoxious.

Output: Updated FloatingMic.tsx with improved visuals and first-visit tooltip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-voice-cta-visibility-make-talk-to-me-unmissable/16-RESEARCH.md
@.planning/phases/16-voice-cta-visibility-make-talk-to-me-unmissable/16-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/components/FloatingMic.tsx:
- Interface: `FloatingMicProps { active: boolean; onToggle: () => void }`
- Default export: `function FloatingMic({ active, onToggle }: FloatingMicProps)`
- fmStyles constant: `{ gold: '#FFD700', red: '#FF4444', bg: '#000' }`
- Current idle box-shadow: `0 0 14px rgba(255,215,0,0.35), 0 12px 28px rgba(0,0,0,0.55)`
- Current border: `1.5px solid` (gold when idle, red when active)
- Current mobile collapse: className `kni-floating-mic` + `kni-floating-mic-label` (globals.css @520px)
- Active animation: `kniFloatPulse 1.4s ease-in-out infinite`

From app/globals.css (added by Plan 01):
- `@keyframes kniMicIdle` — idle gold glow pulse (3s ease-in-out infinite)
- `@media (max-width: 520px) .kni-floating-mic` — currently sets padding:14px, hides label
- `@media (prefers-reduced-motion: reduce)` — suppresses all animations globally

From app/components/VoiceCTA.tsx (created by Plan 01):
- Shares localStorage key: `kni-voice-nudge-seen`
- Sets key to '1' on first mount
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve FloatingMic visuals and add first-visit tooltip</name>
  <files>app/components/FloatingMic.tsx</files>
  <read_first>
    - app/components/FloatingMic.tsx (full current implementation — inline styles, structure, className usage)
    - app/globals.css (confirm kniMicIdle keyframe exists from Plan 01; check @520px responsive rule)
    - .planning/phases/16-voice-cta-visibility-make-talk-to-me-unmissable/16-RESEARCH.md (Pattern 3: FloatingMic improvements, Pattern 4: nudge localStorage, Example 4: tooltip CSS)
  </read_first>
  <action>
    Modify app/components/FloatingMic.tsx with these changes:

    1. Add imports: `useEffect`, `useState` from React.

    2. Add localStorage constant: `const NUDGE_KEY = 'kni-voice-nudge-seen';`

    3. Inside the component function, add nudge state and effect:
       - `const [showTooltip, setShowTooltip] = useState(false)`
       - useEffect on mount: check `localStorage.getItem(NUDGE_KEY)`. If null, `setShowTooltip(true)` and set a 6000ms timeout that calls `setShowTooltip(false)` and `localStorage.setItem(NUDGE_KEY, '1')`. Return cleanup clearing the timeout. If VoiceCTA already set the key (user clicked CTA first), tooltip never shows — this is correct shared behavior.

    4. Visual improvements to the button inline styles (idle state only; active state unchanged):
       - Border: change from `1.5px` to `2px` for more visual weight
       - Background: add subtle gold tint `rgba(255,215,0,0.06)` (was `#000`)
       - Animation (idle): `kniMicIdle 3s ease-in-out infinite` (was `none`). Active state keeps `kniFloatPulse 1.4s ease-in-out infinite`.
       - Minimum dimensions: add `minWidth: 44` and `minHeight: 44` to the button style (WCAG 2.5.8 tap target)

    5. Mobile label change: Replace the `kni-floating-mic-label` span content. Instead of conditionally hiding the label text on mobile via CSS, change the label structure:
       - Desktop label stays: `{active ? 'voice live' : 'talk to me'}`
       - Add a second span with className `kni-floating-mic-short` that shows `{active ? 'live' : 'voice'}` — this span is hidden on desktop and shown on mobile
       - The existing `.kni-floating-mic-label` CSS rule hides the long label at <520px
       - The short label span is hidden by default and shown at <520px (add the responsive rule inline or note for globals.css — but since Plan 01 owns globals.css and is already done, add a `<style>` tag or use inline media query via the existing className pattern)

       SIMPLER APPROACH: Just change the mobile CSS rule. Instead of `display: none` on `.kni-floating-mic-label`, change the label text at the React level. Use a state-based approach: detect viewport width with a simple `useState` + `useEffect` + `matchMedia('(max-width: 520px)')` listener. On narrow screens, render shorter text `voice` / `live`. On wide screens, render full text `talk to me` / `voice live`. This avoids globals.css changes entirely.

    6. First-visit tooltip: When `showTooltip` is true AND `active` is false, render a tooltip div positioned absolutely above the button. The FloatingMic button needs a wrapper div with `position: relative` (or use `position: fixed` on the tooltip itself since the button is fixed-positioned).

       Tooltip implementation: A div with `position: fixed`, bottom calculated as button bottom + button height + 10px gap. Use `right: max(18px, env(safe-area-inset-right, 18px))` to match button alignment. Style: background `#000`, border `1px solid rgba(255,215,0,0.5)`, color `#FFD700`, fontFamily `"Anonymous Pro", monospace`, fontSize `0.75rem`, padding `8px 12px`, whiteSpace `nowrap`, boxShadow `0 0 12px rgba(255,215,0,0.2)`, animation `kniFadeIn 400ms ease-out both` (kniFadeIn already exists in globals.css). Text: "Talk to my AI clone".

    7. On tooltip click or button click when tooltip is showing: dismiss tooltip, set localStorage, then proceed with onToggle.
  </action>
  <verify>
    <automated>cd /Users/kalebnim/Documents/GitHub/nim-kaleb && bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - FloatingMic.tsx imports useState and useEffect from React
    - Contains localStorage read for key `kni-voice-nudge-seen`
    - Idle animation property references `kniMicIdle`
    - Border width is `2px` (was `1.5px`)
    - Background includes `rgba(255,215,0,0.06)` for idle state
    - minWidth and minHeight of 44 set on button style
    - A tooltip element renders conditionally when showTooltip is true
    - Tooltip text contains "Talk to my AI clone"
    - Tooltip has `position: fixed` with bottom/right positioning
    - matchMedia listener for `(max-width: 520px)` controls label text length
    - `bun run build` exits 0
  </acceptance_criteria>
  <done>FloatingMic has idle glow animation, 2px gold border, gold background tint, 44px minimum tap target, responsive short label on mobile, and first-visit tooltip</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 added VoiceCTA (terminal system message) on the home page between the internship banner and directory, wired to open VoiceOverlay. Plan 02 improved FloatingMic with idle glow, larger mobile size, gold tint, and first-visit tooltip. Both share a localStorage key so the nudge fires exactly once.
  </what-built>
  <how-to-verify>
    1. Run `bun dev` and open http://localhost:3000 in a desktop browser
    2. Verify the home page shows a green-bordered voice CTA block between the internship banner and the directory rows
    3. Verify the CTA header reads `[VOICE INTERFACE] ONLINE` and the body mentions "AI voice clone"
    4. Verify the CTA has a gold "start conversation" action
    5. Click "start conversation" — VoiceOverlay should open (same as clicking FloatingMic)
    6. Close the overlay, check the FloatingMic button:
       - Should have a subtle idle gold glow animation
       - Border should be slightly thicker (2px)
       - Should have a faint gold background tint
    7. Open DevTools > Application > Local Storage — confirm `kni-voice-nudge-seen` key exists with value `1`
    8. Clear localStorage, hard refresh — verify the CTA entrance animation plays AND a tooltip appears above FloatingMic
    9. Wait 6 seconds — tooltip should auto-dismiss
    10. Refresh again — neither animation nor tooltip should appear
    11. Resize to 360px width — verify CTA holds without horizontal scroll, FloatingMic shows short "voice" label (not bare dot)
    12. Enable "Reduce Motion" in OS accessibility settings — verify no animations play
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client localStorage | First-visit flag stored client-side; no sensitive data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering | localStorage nudge key | accept | Cosmetic-only flag; tampering resets a tooltip animation — no security impact |
</threat_model>

<verification>
1. `bun run build` exits 0
2. FloatingMic renders with idle glow animation, 2px border, gold tint
3. Mobile (<520px): FloatingMic shows "voice" label, not bare dot
4. First visit: tooltip appears above FloatingMic for ~6s then dismisses
5. Subsequent visits: no tooltip
6. VoiceCTA entrance animation and FloatingMic tooltip share same localStorage key
</verification>

<success_criteria>
- FloatingMic is visually more prominent (glow, border, tint)
- Mobile visitors see "voice" label instead of cryptic dot
- First-visit tooltip auto-dismisses and never repeats
- All visual changes hold at 360px and 1024px+
- prefers-reduced-motion disables all new animations
- Build passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/16-voice-cta-visibility-make-talk-to-me-unmissable/16-02-SUMMARY.md` when done
</output>
