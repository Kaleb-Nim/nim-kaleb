---
phase: 16-voice-cta-visibility-make-talk-to-me-unmissable
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/VoiceCTA.tsx
  - app/components/HomePage.tsx
  - app/page.tsx
  - app/globals.css
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - VOICE-VIS-01
  - VOICE-VIS-03
  - VOICE-VIS-04

must_haves:
  truths:
    - "Home page shows a dedicated voice CTA block that is visually distinct from directory rows"
    - "CTA text clearly communicates you can talk to an AI clone of Kaleb"
    - "Clicking the CTA action opens the VoiceOverlay (same as FloatingMic)"
    - "First-time visitors see an entrance animation on the CTA; repeat visitors do not"
  artifacts:
    - path: "app/components/VoiceCTA.tsx"
      provides: "Voice CTA component with first-visit nudge"
      exports: ["default"]
    - path: "app/components/HomePage.tsx"
      provides: "HomePage with VoiceCTA and onVoiceOpen prop"
    - path: "app/page.tsx"
      provides: "Root page threads onVoiceOpen callback to HomePage"
    - path: "app/globals.css"
      provides: "kniCTAEntrance and kniMicIdle keyframes"
      contains: "kniCTAEntrance"
  key_links:
    - from: "app/page.tsx"
      to: "app/components/HomePage.tsx"
      via: "onVoiceOpen prop passing setVoiceOpen(true)"
      pattern: "onVoiceOpen.*setVoiceOpen"
    - from: "app/components/HomePage.tsx"
      to: "app/components/VoiceCTA.tsx"
      via: "onActivate prop"
      pattern: "onActivate.*onVoiceOpen"
    - from: "app/components/VoiceCTA.tsx"
      to: "localStorage"
      via: "kni-voice-nudge-seen key"
      pattern: "localStorage.*kni-voice-nudge-seen"
---

<objective>
Create the VoiceCTA component (terminal system message style), integrate it into HomePage between the internship banner and directory, wire the onVoiceOpen callback from page.tsx, and add first-visit entrance animation via localStorage tracking. Also add all new keyframes to globals.css and enumerate VOICE-VIS requirements in REQUIREMENTS.md.

Purpose: Visitors who land on the home page will see a prominent, visually distinct voice CTA that clearly communicates the AI voice clone feature — addressing the core discoverability problem.

Output: VoiceCTA.tsx, updated HomePage.tsx with onVoiceOpen prop, updated page.tsx threading the callback, new keyframes in globals.css, VOICE-VIS requirements in REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-voice-cta-visibility-make-talk-to-me-unmissable/16-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/page.tsx (lines 21-24):
- `const route = useHashRoute();` — current hash route string
- `const [voiceOpen, setVoiceOpen] = useState(false);` — voice overlay toggle state
- `<HomePage />` rendered at line 62 with no props currently
- `<FloatingMic active={voiceOpen} onToggle={() => setVoiceOpen((v) => !v)} />` at line 79

From app/components/HomePage.tsx (line 17):
- `export default function HomePage()` — currently takes no props
- Block 3 (internship banner) ends at line 59
- Block 4 (Directory) starts at line 62

From app/globals.css:
- Keyframes defined at lines 63-69 (kniFadeIn, kniBlink, kniBars, kniPanelOpen, kniPageIn, kniRecordPulse, kniFloatPulse)
- prefers-reduced-motion rule at lines 75-83 (covers all animations globally)
- FloatingMic responsive collapse at lines 138-141

From app/components/FloatingMic.tsx:
- Interface: `{ active: boolean; onToggle: () => void }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add keyframes to globals.css and enumerate VOICE-VIS requirements</name>
  <files>app/globals.css, .planning/REQUIREMENTS.md</files>
  <read_first>
    - app/globals.css (current keyframes block at lines 63-69, reduced-motion rule at 75-83)
    - .planning/REQUIREMENTS.md (append new section after Hackathons Phase 15 requirements)
  </read_first>
  <action>
    In app/globals.css, append two new keyframes after the existing `kniFloatPulse` keyframe (line 69) and before the `.kni-page` rule (line 71):

    1. `@keyframes kniCTAEntrance` — entrance animation for VoiceCTA first-visit nudge. Animates from opacity:0 + translateY(8px) to opacity:1 + translateY(0) with a green box-shadow bloom at 100% (`0 0 20px rgba(0,255,0,0.15), inset 0 0 12px rgba(0,255,0,0.05)`). Duration applied in component: 600ms ease-out both.

    2. `@keyframes kniMicIdle` — subtle idle glow for FloatingMic (consumed by Plan 02). Pulses box-shadow between `0 0 10px rgba(255,215,0,0.25)` at 0%/100% and `0 0 22px rgba(255,215,0,0.5)` at 50%. Both keyframes keep the existing `0 12px 28px rgba(0,0,0,0.55)` drop shadow layer.

    No changes needed for prefers-reduced-motion — the existing global rule at lines 75-83 already suppresses all animations.

    In .planning/REQUIREMENTS.md, add a new section `### Voice CTA Visibility (Phase 16)` after the Hackathons Phase 15 section. Enumerate:
    - VOICE-VIS-01: Home page includes a dedicated voice CTA element visually distinct from directory rows that communicates "you can talk to an AI clone of Kaleb"
    - VOICE-VIS-02: FloatingMic button is more eye-catching — improved contrast, size, or visual treatment
    - VOICE-VIS-03: First-time visitors see a one-time attention nudge that does not repeat on subsequent visits
    - VOICE-VIS-04: Voice CTA click/tap triggers the same VoiceOverlay connection flow as FloatingMic

    Also update the Traceability table to add `VOICE-VIS-01..04 | Phase 16 | Planned` and update the Coverage count.
  </action>
  <verify>
    <automated>grep -c 'kniCTAEntrance\|kniMicIdle' app/globals.css | grep -q '2' && grep -c 'VOICE-VIS-0[1-4]' .planning/REQUIREMENTS.md | grep -q '[4-9]' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - app/globals.css contains `@keyframes kniCTAEntrance` with opacity + translateY + box-shadow properties
    - app/globals.css contains `@keyframes kniMicIdle` with box-shadow pulse between 0.25 and 0.5 alpha gold
    - .planning/REQUIREMENTS.md contains VOICE-VIS-01 through VOICE-VIS-04 entries
    - Traceability table includes `VOICE-VIS-01..04 | Phase 16`
  </acceptance_criteria>
  <done>Both keyframes exist in globals.css and all four VOICE-VIS requirements enumerated in REQUIREMENTS.md</done>
</task>

<task type="auto">
  <name>Task 2: Create VoiceCTA component with first-visit nudge</name>
  <files>app/components/VoiceCTA.tsx</files>
  <read_first>
    - app/components/HomePage.tsx (inline style patterns — fontSize clamp, color values, textShadow patterns)
    - app/components/Directory.tsx (directory row styling for contrast — VoiceCTA must look different)
    - app/globals.css (confirm kniCTAEntrance keyframe exists from Task 1)
    - .planning/phases/16-voice-cta-visibility-make-talk-to-me-unmissable/16-RESEARCH.md (Pattern 1 Approach A, Pattern 4 localStorage)
  </read_first>
  <action>
    Create app/components/VoiceCTA.tsx as a new file. Use `'use client'` directive.

    Interface: `VoiceCTAProps { onActivate?: () => void }`. Default export function `VoiceCTA`.

    Design: Terminal System Message style (Research Approach A per VOICE-VIS-01). Structure:
    - Outer container: border `1px solid rgba(0,255,0,0.25)`, borderLeft `3px solid #00FF00`, background `rgba(0,255,0,0.04)`, padding `clamp(14px, 3vw, 20px)`, fontFamily `"Anonymous Pro", monospace`. Margin: `14px 0 18px` (matches .kni-banner-row margin pattern).
    - Header line: `[VOICE INTERFACE]` in `#00FF00` with strong glow (`0 0 6px rgba(0,255,0,0.55), 0 0 14px rgba(0,255,0,0.25)`), followed by ` ● ONLINE` in `#27C93F`. Font size `clamp(0.72rem, 2vw, 0.82rem)`, fontWeight 700.
    - Body text: "Talk to my AI voice clone -- it knows my work, my projects, and answers like I would." in `rgba(0,255,0,0.7)`, fontSize `clamp(0.74rem, 2vw, 0.82rem)`, lineHeight 1.6, marginBottom 12.
    - Action button: `<button>` element (not anchor) with text `▸ start conversation`. Color `#FFD700`, gold textShadow, fontSize `clamp(0.78rem, 2vw, 0.88rem)`, fontWeight 700, background none, border none, cursor pointer, padding `8px 0`, letterSpacing `0.04em`. onClick calls `handleActivate`.

    First-visit nudge (per VOICE-VIS-03):
    - localStorage key: `kni-voice-nudge-seen`
    - State: `const [showNudge, setShowNudge] = useState(false)`
    - useEffect on mount: check `localStorage.getItem('kni-voice-nudge-seen')`. If null, `setShowNudge(true)` and `localStorage.setItem('kni-voice-nudge-seen', '1')`. No auto-dismiss timer needed (animation plays once and holds).
    - Outer container gets `animation: showNudge ? 'kniCTAEntrance 600ms ease-out both' : 'none'`
    - `handleActivate` function: calls `setShowNudge(false)` then `onActivate?.()`

    Follow existing inline style pattern (same as HomePage, FloatingMic — no CSS modules for this component).
  </action>
  <verify>
    <automated>grep -c 'kni-voice-nudge-seen' app/components/VoiceCTA.tsx | grep -q '[2-9]' && grep -c 'onActivate' app/components/VoiceCTA.tsx | grep -q '[1-9]' && grep -c 'VOICE INTERFACE' app/components/VoiceCTA.tsx | grep -q '[1-9]' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - app/components/VoiceCTA.tsx exports a default function component named VoiceCTA
    - Props interface has `onActivate?: () => void`
    - Contains localStorage read/write for key `kni-voice-nudge-seen` inside a useEffect
    - Contains `[VOICE INTERFACE]` and `ONLINE` text in the header
    - Contains a button with text `start conversation` and onClick handler
    - Animation property references `kniCTAEntrance` when showNudge is true
    - No `import` of CSS modules (follows inline style convention)
  </acceptance_criteria>
  <done>VoiceCTA.tsx created with terminal system message design, first-visit localStorage nudge, and onActivate callback wiring</done>
</task>

<task type="auto">
  <name>Task 3: Wire VoiceCTA into HomePage and thread onVoiceOpen from page.tsx</name>
  <files>app/components/HomePage.tsx, app/page.tsx</files>
  <read_first>
    - app/components/HomePage.tsx (current structure — no props, Blocks 1-5)
    - app/page.tsx (line 24: voiceOpen state, line 62: HomePage rendered with no props)
    - app/components/VoiceCTA.tsx (confirm it exports default and accepts onActivate prop)
  </read_first>
  <action>
    In app/components/HomePage.tsx:
    1. Add import: `import VoiceCTA from './VoiceCTA';`
    2. Add props interface: `interface HomePageProps { onVoiceOpen?: () => void; }`
    3. Change signature from `function HomePage()` to `function HomePage({ onVoiceOpen }: HomePageProps)`
    4. Insert `<VoiceCTA onActivate={onVoiceOpen} />` between Block 3 (internship banner, after the closing `</div>` of `.kni-banner-row` at line 59) and Block 4 (Directory at line 62).
    5. No changes to Blocks 1, 2, 3, 4, or 5 content.

    In app/page.tsx:
    1. Change `<HomePage />` (line 62) to `<HomePage onVoiceOpen={() => setVoiceOpen(true)} />`
    2. No other changes. The existing FloatingMic and VoiceOverlay wiring remain untouched.

    Per VOICE-VIS-04: the VoiceCTA click path is: VoiceCTA button onClick -> onActivate -> onVoiceOpen -> setVoiceOpen(true) -> useEffect triggers voice.connect() -> VoiceOverlay renders. Identical to FloatingMic path.
  </action>
  <verify>
    <automated>cd /Users/kalebnim/Documents/GitHub/nim-kaleb && bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/components/HomePage.tsx imports VoiceCTA from './VoiceCTA'
    - HomePage function signature accepts `{ onVoiceOpen }: HomePageProps`
    - VoiceCTA rendered between Block 3 (banner) and Block 4 (Directory) with `onActivate={onVoiceOpen}`
    - app/page.tsx passes `onVoiceOpen={() => setVoiceOpen(true)}` to HomePage
    - `bun run build` exits 0 with no TypeScript errors
  </acceptance_criteria>
  <done>VoiceCTA wired into HomePage, onVoiceOpen callback threaded from page.tsx, build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client localStorage | First-visit flag stored client-side; no sensitive data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | localStorage `kni-voice-nudge-seen` | accept | Flag controls a cosmetic animation only; tampering has zero security impact |
| T-16-02 | Information Disclosure | VoiceCTA copy text | accept | Text is intentionally public marketing copy; no secrets |
</threat_model>

<verification>
1. `bun run build` exits 0
2. Home page renders VoiceCTA between internship banner and directory
3. Clicking "start conversation" opens VoiceOverlay
4. First visit: VoiceCTA has entrance animation; subsequent visits: no animation
5. `localStorage.getItem('kni-voice-nudge-seen')` returns '1' after first visit
</verification>

<success_criteria>
- VoiceCTA component exists and renders on home page
- CTA is visually distinct from directory rows (green left-accent border, different background)
- CTA copy says "Talk to my AI voice clone"
- CTA action triggers VoiceOverlay via setVoiceOpen(true)
- First-visit entrance animation plays once and does not repeat
- Build passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/16-voice-cta-visibility-make-talk-to-me-unmissable/16-01-SUMMARY.md` when done
</output>
