---
phase: quick-260708-j9l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/robots.ts
  - app/sitemap.ts
autonomous: true
requirements:
  - QUICK-260708-j9l
must_haves:
  truths:
    - "Rendered <head> exposes a human-readable meta description that names Kaleb Nim and his role (AI Engineer, Singapore, NUS Business AI Systems, Singapore Youth AI)"
    - "Rendered <head> exposes Open Graph and Twitter card tags with title, description, and canonical URL https://www.kalebnim.dev"
    - "Rendered <head> exposes a canonical link resolving to https://www.kalebnim.dev"
    - "Rendered document body contains a Person JSON-LD script whose sameAs lists the LinkedIn and GitHub profile URLs"
    - "GET /robots.txt is served and references the sitemap at https://www.kalebnim.dev/sitemap.xml"
    - "GET /sitemap.xml is served and lists exactly the root URL https://www.kalebnim.dev"
  artifacts:
    - app/layout.tsx
    - app/robots.ts
    - app/sitemap.ts
  key_links:
    - "metadataBase is set to https://www.kalebnim.dev so relative canonical/OG URLs resolve to absolute"
    - "robots.ts sitemap URL matches the file-based sitemap route (/sitemap.xml)"
    - "JSON-LD is serialized with JSON.stringify from a static object (no user input) to avoid injection"
---

<objective>
Add SEO foundations so the site ranks for the keyword "Kaleb Nim": rewrite the root metadata to be human-readable and keyword-relevant, add Open Graph / Twitter cards, a canonical URL, and metadataBase; embed Person JSON-LD structured data with sameAs profile links; and add file-based robots and sitemap routes using Next.js 16 App Router conventions.

Purpose: The current metadata is sci-fi flavor text ("Operating Model: kaleb-nim-400b-0706") with no description of who Kaleb Nim is, no social cards, no structured data, no robots.txt, and no sitemap. This hurts discoverability and how the site appears when shared.
Output: Updated `app/layout.tsx` (metadata + JSON-LD), new `app/robots.ts`, new `app/sitemap.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@app/layout.tsx

Established facts (verified by orchestrator — do not re-research):
- Canonical host: https://www.kalebnim.dev (includes www)
- Single-page app with hash routing (#/, #/work-experience) — hash fragments are NOT indexable URLs, so the sitemap lists only the root URL
- Person profile URLs for sameAs: https://www.linkedin.com/in/kaleb-nim/ and https://github.com/Kaleb-Nim
- Who Kaleb Nim is: AI Engineer from Singapore, studying Business AI Systems at NUS, Head of Community at Singapore Youth AI, runs an AI voice-clone portfolio
- Next.js 16.0.10 App Router; robots.ts / sitemap.ts return MetadataRoute.Robots / MetadataRoute.Sitemap
- Keep terminal UI and all existing components untouched — this is metadata/SEO-file work only
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite root metadata and embed Person JSON-LD in app/layout.tsx</name>
  <files>app/layout.tsx</files>
  <action>
    Replace the exported `metadata` object with a keyword-relevant, human-readable version. Set `metadataBase: new URL("https://www.kalebnim.dev")`. Keep `title` leading with the keyword "Kaleb Nim" (e.g. "Kaleb Nim | AI Engineer & AI Voice Clone Portfolio") — the keyword must appear first. Set a human-readable `description` (~150-160 chars) that names who Kaleb Nim is using the established facts: AI Engineer from Singapore, studying Business AI Systems at NUS, Head of Community at Singapore Youth AI, with an AI voice-clone portfolio you can talk to. Add `alternates: { canonical: "/" }` (resolves against metadataBase to https://www.kalebnim.dev). Add an `openGraph` block with `title`, `description`, `url: "https://www.kalebnim.dev"`, `siteName: "Kaleb Nim"`, `type: "website"`, `locale: "en_US"`. Add a `twitter` block with `card: "summary_large_image"`, plus `title` and `description`. Reuse the same title/description strings across metadata, openGraph, and twitter for consistency (define them as local consts if it reads cleaner).

    Add Person JSON-LD structured data. Define a plain object with keys `@context: "https://schema.org"`, `@type: "Person"`, `name: "Kaleb Nim"`, `url: "https://www.kalebnim.dev"`, `jobTitle: "AI Engineer"`, and `sameAs: ["https://www.linkedin.com/in/kaleb-nim/", "https://github.com/Kaleb-Nim"]`. Render it inside the returned JSX (in the <body>, before {children}) as a script element with `type="application/ld+json"` using `dangerouslySetInnerHTML={{ __html: JSON.stringify(personJsonLd) }}`. The object is a static compile-time constant with no user input, so JSON.stringify is safe here. Do NOT touch the font setup, the Analytics component, the html/body wrapper classes, or any other file.
  </action>
  <verify>
    <automated>grep -q "metadataBase" app/layout.tsx && grep -q "application/ld+json" app/layout.tsx && grep -q "kaleb-nim" app/layout.tsx && grep -q "summary_large_image" app/layout.tsx && bun run build</automated>
  </verify>
  <done>app/layout.tsx exports metadata with metadataBase, human-readable keyword description, canonical, openGraph, and twitter; the body renders a Person JSON-LD script with LinkedIn + GitHub sameAs links; `bun run build` succeeds with no type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add file-based robots.ts and sitemap.ts routes</name>
  <files>app/robots.ts, app/sitemap.ts</files>
  <action>
    Create `app/robots.ts` exporting a default function `robots(): MetadataRoute.Robots` (import the type via `import type { MetadataRoute } from "next"`). Return an object whose `rules` allow all crawlers (`userAgent: "*"`, `allow: "/"`) and whose `sitemap` points to `"https://www.kalebnim.dev/sitemap.xml"`. Do not disallow any paths — the whole public site should be crawlable.

    Create `app/sitemap.ts` exporting a default function `sitemap(): MetadataRoute.Sitemap` (same type import). Return an array with a single entry for the root URL only: `url: "https://www.kalebnim.dev"`, `lastModified: new Date()`, `changeFrequency: "monthly"`, `priority: 1`. Do NOT add hash-route entries (#/work-experience etc.) — hash fragments are not independently indexable URLs, so the root is the only canonical page.
  </action>
  <verify>
    <automated>test -f app/robots.ts && test -f app/sitemap.ts && grep -q "MetadataRoute.Robots" app/robots.ts && grep -q "MetadataRoute.Sitemap" app/sitemap.ts && grep -q "sitemap.xml" app/robots.ts && bun run build</automated>
  </verify>
  <done>app/robots.ts and app/sitemap.ts exist and return the correct MetadataRoute types; the sitemap lists only https://www.kalebnim.dev; robots references the sitemap; `bun run build` succeeds and emits the /robots.txt and /sitemap.xml routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client HTML | JSON-LD is injected into the document via dangerouslySetInnerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Injection (XSS) | Person JSON-LD script in app/layout.tsx | low | accept | JSON-LD is built from a static, hardcoded object with no user/runtime input and serialized via JSON.stringify; there is no untrusted data path, so dangerouslySetInnerHTML carries no injection risk. |
</threat_model>

<verification>
- `bun run build` completes without type errors or build failures.
- app/layout.tsx `<head>` output contains description, canonical, og:*, and twitter:* tags; body contains the ld+json Person script with both sameAs URLs.
- Built app serves /robots.txt (references sitemap) and /sitemap.xml (root URL only).
- No changes to terminal UI components, hooks, or any file other than the three listed.
</verification>

<success_criteria>
- Root metadata is human-readable, leads with the "Kaleb Nim" keyword, and includes metadataBase, canonical, Open Graph, and Twitter cards.
- Person JSON-LD with LinkedIn + GitHub sameAs links renders in the document.
- robots.txt and sitemap.xml are generated via App Router file conventions, sitemap listing only https://www.kalebnim.dev.
- `bun run build` passes.
</success_criteria>

<output>
Create `.planning/quick/260708-j9l-add-seo-foundations-for-keyword-kaleb-ni/260708-j9l-SUMMARY.md` when done.
</output>
